// ────────────────────────────────────────────────────────────────
//  Frontend configuration — EDIT THIS ONE LINE after deploying the API.
//
//  Set API_BASE to your backend's public URL (your VPS domain), with NO
//  trailing slash. Examples:
//     window.API_BASE = "https://api.yourdomain.com";
//     window.API_BASE = "http://123.45.67.89";        // raw IP (dev/testing)
//
//  Local dev default below points at gunicorn's default port (8000).
//  Leave it "" to call the same origin (only useful for the old monolith).
// ────────────────────────────────────────────────────────────────
window.API_BASE = "http://localhost:8000";
