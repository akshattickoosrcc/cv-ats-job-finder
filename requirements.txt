Flask>=3.0.0
Flask-Limiter>=3.5.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# PDF: validation (pypdf) + fast extraction (PyMuPDF; AGPL — see pdftext.py) +
# pdfplumber fallback.
pypdf>=4.0.0
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# Scraper
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Queue backend (Redis in prod; SQLite fallback needs no package)
redis>=5.0.0

# Scheduling (worker periodic scrape) + misc
APScheduler>=3.10.0
cachetools>=5.3.0
